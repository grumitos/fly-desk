/** Canonical public limits shared by validation and the runtime UI contract. */
export const MAX_FLEXIBLE_STAY_NIGHTS = 90;
export const MAX_SEARCH_PASSENGERS = 9;
export const MAX_LAP_INFANTS_PER_ADULT = 1;
