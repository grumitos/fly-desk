{
  "name": "travel-quote-foundation",
  "private": true,
  "version": "0.1.0",
  "description": "Base tecnica inicial para un motor de cotizacion aerea para agentes de viajes.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "demo": "tsx src/demo.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No configurado todavia\"",
    "test": "echo \"No configurado todavia\""
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "playwright": "^1.58.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
